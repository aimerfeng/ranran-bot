修复：外部人设被内置规则盖掉，改为彻底独立的两套提示词栈

现象：/nsfw on 之后模型仍然只给 150 字左右的温和回复，完全没有按人设写。
根因（都是拼装方式的问题，不是人设本身）：
1. OUTPUT_GUARD 落在系统提示词最末尾且声明覆盖一切，其中明确禁止「内心独白、
   星号包裹的点评」——正是人设要求的「每段一处 *斜体* 内心独白」；
2. 内置然然人设排在最前（动作只作点缀、闲聊 80~220 字、群友口吻），语气被它主导；
3. 用户侧提示词仍是助手式（先理解意图再决定语气），进一步压短回复；
4. skill 目录、联网搜索规则也一并注入，对人设只起稀释作用。

改法：
- 角色扮演模式独立成栈：整段系统提示词 = 外部人设 + 运行环境约束；内置人设、skill、
  输出纪律、搜索规则全部不注入；answer() 会丢弃调用方传来的 extra/guard，
  防止输出纪律重新落到末尾；
- 用户侧提示词换成前情提要式（build_roleplay_prompt），不再有助手式元指令；
- 角色扮演模式下长回复不再转 md 文件（长篇剧情是常态，转文件破坏沉浸感）；
- /persona 与 /nsfw 显示当前实际生效的是哪套栈；启动日志同步更新。

验证（Telegram 适配层 + 真实模型，群 -1003946203848）：
  修复前 151 字 / 8 句 / 无内容；修复后 2170 字 / 3 处斜体内心独白 / 露骨用词 12 次 / 无拒答
  185 个单元测试通过（新增角色扮演栈与投递行为回归测试）
